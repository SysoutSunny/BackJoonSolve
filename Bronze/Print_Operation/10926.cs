using System;
using static System.Console;

namespace BackJoon
{
    class _10926
    {
        static void Main()
        {
            string s = ReadLine();
            WriteLine(s + "??!"); 
            // 한 줄로 쓸 수 있음 (WriteLine(ReadLine() + "??!");)
        }
    }
}